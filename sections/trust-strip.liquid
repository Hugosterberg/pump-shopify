{% comment %}
  Trust strip — horizontal row of trust signals with premium pill styling.
{% endcomment %}

<div
  class="trust-strip full-width pp-accent-line"
  style="
    --ts-bg: {{ section.settings.background_color }};
    --ts-text: {{ section.settings.text_color }};
    --ts-muted: {{ section.settings.muted_text_color }};
    --ts-accent: {{ section.settings.accent_color }};
  "
>
  <div class="trust-strip__inner">
    {% if section.blocks.size > 0 %}
      <ul class="trust-strip__list" role="list">
        {% for block in section.blocks %}
          <li class="trust-strip__item" {{ block.shopify_attributes }}>
            <span class="trust-strip__icon" aria-hidden="true">
              {% render 'icon', name: block.settings.icon | default: 'check', size: 16 %}
            </span>
            <span class="trust-strip__text">{{ block.settings.text }}</span>
          </li>
        {% endfor %}
      </ul>
    {% endif %}
  </div>
</div>

{% stylesheet %}
  .trust-strip {
    position: relative;
    padding-block: 18px;
    padding-inline: var(--page-margin, 20px);
    background-color: var(--ts-bg, #101820);
    color: var(--ts-text, #f6f7f2);
  }

  .trust-strip__inner {
    max-width: var(--page-width, 90rem);
    margin-inline: auto;
  }

  .trust-strip__list {
    list-style: none;
    margin: 0;
    padding: 0;
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 10px;
  }

  .trust-strip__item {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 8px 14px;
    border-radius: 999px;
    font-size: clamp(0.76rem, 2.2vw, 0.82rem);
    font-weight: 600;
    line-height: 1.3;
    color: var(--ts-text, #f6f7f2);
    background: color-mix(in srgb, var(--ts-text, #f6f7f2) 5%, transparent);
    border: 1px solid color-mix(in srgb, var(--ts-text, #f6f7f2) 9%, transparent);
    transition: border-color 0.2s ease, background 0.2s ease, transform 0.2s ease;
  }

  .trust-strip__item:hover {
    transform: translateY(-1px);
    border-color: color-mix(in srgb, var(--ts-accent, #2dd4bf) 35%, transparent);
    background: color-mix(in srgb, var(--ts-accent, #2dd4bf) 8%, transparent);
  }

  .trust-strip__icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 28px;
    height: 28px;
    border-radius: 999px;
    color: var(--ts-accent, #2dd4bf);
    background-color: color-mix(in srgb, var(--ts-accent, #2dd4bf) 16%, transparent);
  }

  .trust-strip__text {
    text-wrap: balance;
    text-align: left;
  }

  @media screen and (min-width: 750px) {
    .trust-strip__list {
      gap: 12px 14px;
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "Trust strip",
  "tag": "section",
  "settings": [
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#101820"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text",
      "default": "#F6F7F2"
    },
    {
      "type": "color",
      "id": "muted_text_color",
      "label": "Muted text",
      "default": "#9AA8A2"
    },
    {
      "type": "color",
      "id": "accent_color",
      "label": "Accent",
      "default": "#2DD4BF"
    }
  ],
  "blocks": [
    {
      "type": "trust_item",
      "name": "Trust item",
      "settings": [
        {
          "type": "select",
          "id": "icon",
          "label": "Icon",
          "options": [
            { "value": "check", "label": "Check" },
            { "value": "truck", "label": "Truck / shipping" },
            { "value": "shield", "label": "Shield / warranty" },
            { "value": "bolt", "label": "Bolt / charge" },
            { "value": "usb", "label": "USB-C" },
            { "value": "battery", "label": "Battery" },
            { "value": "card", "label": "Card / payment" },
            { "value": "coins", "label": "Coins / payment" },
            { "value": "lock", "label": "Lock / secure" },
            { "value": "leaf", "label": "Leaf" }
          ],
          "default": "check"
        },
        {
          "type": "text",
          "id": "text",
          "label": "Text",
          "default": "Fri frakt"
        }
      ]
    }
  ],
  "max_blocks": 5,
  "presets": [
    {
      "name": "Trust strip",
      "blocks": [
        { "type": "trust_item", "settings": { "icon": "truck", "text": "Fri frakt över 999 kr" } },
        { "type": "trust_item", "settings": { "icon": "shield", "text": "14 dagars ångerrätt" } },
        { "type": "trust_item", "settings": { "icon": "coins", "text": "Säker betalning" } },
        { "type": "trust_item", "settings": { "icon": "usb", "text": "USB-C laddning" } }
      ]
    }
  ]
}
{% endschema %}
